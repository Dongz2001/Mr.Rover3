<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#D9D9D9" /> <!-- Background color -->
            <corners android:radius="10dp" /> <!-- Corner radius if needed -->
        </shape>
    </item>
    <item android:left="5dp" android:right="5dp" android:top="5dp" android:bottom="5dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" /> <!-- Transparent to show the background -->
            <stroke
                android:width="5dp"
                android:color="#D9D9D9" /> <!-- Border color -->
            <corners android:radius="10dp" /> <!-- Same corner radius as the background -->
        </shape>
    </item>
</layer-list>