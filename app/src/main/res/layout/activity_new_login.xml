<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/newLogin"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    tools:context=".newLogin">


    <LinearLayout
        android:id="@+id/image_container"
        android:layout_width="match_parent"
        android:layout_height="400dp"
        android:background="@color/my_secondary"
        android:gravity="center">

        <ImageView
            android:id="@+id/imageView2"
            android:layout_width="250dp"
            android:layout_height="250dp"
            app:srcCompat="@drawable/logo" />
    </LinearLayout>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome, Kalovers!"
        android:letterSpacing="0.08"
        android:textStyle="bold"
        android:textSize="25sp"
        android:textColor="@color/black"
        android:layout_marginLeft="35dp"
        android:layout_marginTop="20dp"
        android:layout_below="@id/image_container"
        android:id="@+id/textView"/>

    <!-- Username EditText positioned below the TextView -->
    <EditText
        android:layout_width="335dp"
        android:layout_height="55dp"
        android:layout_centerHorizontal="true"
        android:inputType="textEmailAddress"
        android:hint="Username"
        android:background="@drawable/textfield"
        android:drawableLeft="@drawable/login_icon"
        android:paddingStart="10dp"
        android:layout_marginTop="30dp"
        android:layout_below="@id/textView"
        android:id="@+id/editTextUsername"/>

    <!-- Password EditText positioned below the Username EditText -->
    <EditText
        android:layout_width="335dp"
        android:layout_height="55dp"
        android:layout_centerHorizontal="true"
        android:inputType="textPassword"
        android:hint="Password"
        android:background="@drawable/textfield"
        android:drawableLeft="@drawable/lock"
        android:paddingStart="10dp"
        android:layout_marginTop="12dp"
        android:layout_below="@id/editTextUsername"
        android:id="@+id/editTextPassword"/>

    <!-- Forgot Password TextView positioned below the Password EditText -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="35dp"
        android:text="Forgot Password?"
        android:textColor="@color/black"
        android:textSize="15sp"
        android:layout_below="@id/editTextPassword"
        android:id="@+id/textViewForgotPassword"/>

    <!-- Login Button positioned below the Forgot Password TextView -->
    <Button
        android:layout_width="335dp"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="LOGIN"
        android:background="@drawable/button_color"
        android:textColor="@color/white"
        android:textSize="16dp"
        android:id="@+id/loginBTN"
        android:layout_marginTop="35dp"
        android:layout_below="@id/textViewForgotPassword" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Don't have an account?"
        android:layout_marginTop="12dp"
        android:layout_marginLeft="90dp"
        android:textColor="@color/black"
        android:layout_below="@id/loginBTN"
        android:id="@+id/donthave"
        android:layout_marginRight="10dp"
        />


    <CheckBox
        android:id="@+id/checkbox_remember_me"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Remember Me"
        android:layout_below="@id/editTextPassword"
        android:layout_marginTop="5dp"
        android:layout_marginLeft="40dp"/>

    <TextView
        android:id="@+id/register_here"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Register here"
        android:layout_marginTop="12dp"
        android:textColor="@color/blue"
        android:layout_below="@id/loginBTN"
        android:layout_toRightOf="@id/donthave"
        android:layout_marginEnd="8dp"/>
    />

</RelativeLayout>